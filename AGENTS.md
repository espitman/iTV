# دستورهای پروژه iTV

- برای پیاده‌سازی و تغییر کد این پروژه از Cursor CLI با مدل Grok 4.6 Fast استفاده کن.
- ارتباط با Cursor CLI باید از مسیر ACP (Agent Client Protocol) باشد.
- مدل انتخاب‌شده را از پاسخ `session/new` در ACP بررسی کن؛ نام مدل یا روش اجرا را بدون تأیید واقعی گزارش نکن.
- انتشار بعدی فقط با اسکریپت چک‌این‌شده `scripts/release.sh` انجام شود.
- در هر انتشار بعدی باید هم `versionCode` و هم `versionName` افزایش یابد؛ نام فایل APK انتشار از همان `versionName` ساخته می‌شود (`release/iTV-v<versionName>-release.apk`).

## Graphify — فقط همین پروژه

- برای سؤال‌های ساختار کد، وابستگی‌ها و اثر تغییرات، ابتدا گراف محلی `graphify-out/graph.json` را با `graphify query`، `graphify explain`، `graphify path` یا `graphify affected` بررسی کن. فرمان‌ها را از ریشهٔ همین پروژه اجرا کن و `--graph graphify-out/graph.json` بده.
- نتیجه‌های مهم را پیش از پاسخ یا تغییر با فایل اصلی و شمارهٔ خط بررسی کن. رابطهٔ import، reference و call را یکی فرض نکن؛ نبودن رابطه در گراف اثبات نبودن وابستگی نیست.
- اگر کد پس از ساخت گراف تغییر کرده یا خروجی موجود نیست، از ریشهٔ پروژه با `graphify extract . --code-only --no-cluster` و سپس `graphify cluster-only . --no-label` گراف را بازسازی کن. فایل‌های مستثنا در `.graphifyignore` مشخص‌اند.
- استخراج فعلی مبتنی بر کد است؛ XML، Manifest، منابع اندروید، تنظیمات بیلد و وابستگی‌های پویا ممکن است در گراف نباشند یا کامل نباشند و باید مستقیم بررسی شوند.
- فعال‌سازی محدود به همین پروژه است: تنظیمات سراسری، نصب مهارت سراسری، ادغام با گراف سراسری و watcher خودکار ایجاد نکن.
- راهنمای فرمان‌ها و نتیجهٔ بررسی اولیه در `docs/GRAPHIFY.md` است.
